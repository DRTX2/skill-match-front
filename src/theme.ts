import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0077B5", // Color de LinkedIn
    },
    secondary: {
      main: "#00A0DC",
    },
  },
  typography: {
    fontFamily: "Arial, sans-serif",
  },
});

export default theme;
